#include <stdio.h>

int main(void)
{
    int ftemp, ctemp;

    printf("Enter the temperature in Fahrenheit: ");
    scanf("%d", &ftemp);

    ctemp = ftemp

    print("Celsius equivalent: %d\n", ctemp);
}